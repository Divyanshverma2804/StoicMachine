version: "3.9"

services:
  reelforge:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persist DB, rendered videos, stock images, YT token
      - ./app/templates:/app/app/templates   #No rebuild needed, templates are live-mounted # Just pull and the changes are instantly live
      - ./data:/app/data
      - ./output:/app/output
      - ./stock:/app/stock
      - ./music.mp3:/app/music.mp3:ro
      # Mount your YT client_secret.json
      - ./client_secret.json:/app/client_secret.json:ro
      - chatterbox_cache:/root/.cache/huggingface
      - ./voice_ref.wav:/app/voice_ref.wav:ro
    # Limit render concurrency — one job at a time is handled by the scheduler
    # but you can scale workers here later
    deploy:
      resources:
        limits:
          memory: 8g
volumes:
  chatterbox_cache: